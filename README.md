# Java-Graph-Cycle-Detection
Graph Cycle Detector is a Java project that detects cycles in directed graphs using Depth First Search (DFS). It reads graph data from input files, builds an adjacency list, and checks for cyclic paths. The project demonstrates core concepts of graph theory, recursion, data structures, and object-oriented programming.
